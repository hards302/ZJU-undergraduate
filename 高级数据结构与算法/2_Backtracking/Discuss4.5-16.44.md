```c
int booking[解数][999999];
len = M;
recur(int*  a, int choice, int len)
{
    a[pos] = choice;
    if(is_sol()) {a->booking; return;}
    
    pos++;
    recur(a,0,pos);
    if(keyifang) recur(a,1,pos);
	
}


fruit[999];
fruit[id] = price;
a = 110(-1)00100101111;

calculate_price(int* a)	
{
    sum = 0;
    for(int i=0; i<len;i++){
        if(a[i] == 1) sum += fruit[i];
    }
}


```

